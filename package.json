{
  "name": "ai-evolutionx-platform-2025",
  "version": "1.0.0",
  "description": "Advanced AI Platform with Continuous Learning",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd frontend && npm install",
    "build": "cd frontend && npm run build && mkdir -p ../www && cp -r dist/* ../www/",
    "sync": "npx cap sync android",
    "android": "npx cap sync android && npx cap open android"
  },
  "keywords": ["ai", "chat", "ollama"],
  "author": "AI EvolutionX Team",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
